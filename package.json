{
  "name": "les-enfants-d-adam-main",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd backend && npm run start",
    "dev": "cd frontend && npm run dev",
    "start:both": "concurrently \"npm:dev\" \"npm:start\"",
    "build:render": "cd frontend && npm ci && npm run build && cd ../backend && npm ci"
  },
  "dependencies": {
    "concurrently": "^8.2.0",
    "pptxgenjs": "^4.0.1",
    "xlsx": "^0.18.5"
  }
}
